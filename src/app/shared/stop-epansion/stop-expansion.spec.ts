import { StopExpansion } from './stop-expansion';

describe('StopExpansion', () => {
  it('should create an instance', () => {
    const directive = new StopExpansion();
    expect(directive).toBeTruthy();
  });
});
